git:clone() {
    local repo="$1"
    local dest="$2"

    if [ -z "$repo" ] || [ -z "$dest" ]; then
        echo "Usage: git:repo:clone <repo-name> <directory>"
        return 1
    fi

    git clone -b main "git@localhost:/home/git/repos/${repo}.git" "$dest"
}
