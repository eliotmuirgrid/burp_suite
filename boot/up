# Super important to know where you are in the file system
PROMPT='%m:%~ %# '
FLOW_SHELL=$HOME/flowshell
FLOW_COMMANDS=$FLOW_SHELL/commands
export LUA_PATH="$FLOW_SHELL/nvim/?.lua"

source "$FLOW_COMMANDS/command:install"
command:install
