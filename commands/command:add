command:add() {
   local name="$1"
   arg:check 1 "@" || return 1
   local file="$CMD_DIR/$name"
   if [[ ! -e "$file" ]]; then
      command:boiler:plate "$file" "$name"
      chmod +x "$file"
      echo "Created $file"
   fi

   edit "$file"
   source $file
   command:publish "Added $name" 
}
