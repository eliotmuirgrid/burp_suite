string:split() {
    typeset -n out="$1"
    local string="$2"
    local sep="${3:-,}"

    out=()

    while [[ "$string" == *"$sep"* ]]; do
        out+=("${string%%"$sep"*}")
        string="${string#*"$sep"}"
    done

    out+=("$string")
}
