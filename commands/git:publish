git:publish() {
   local msg="${*:-Update}"
   local root="$HOME/$(pwd:top)"
   echo "$root" 

   git -C $root add . || return
   git -C $root commit -m "$msg" || return

   git pull --no-edit:wq
   git:push:all
}
