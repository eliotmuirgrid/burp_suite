git:publish() {
   local msg="${arg:default $1 Update}"
   local default_root="$HOME/$(pwd:top)"
   local root="${arg:default $2 $default_root}"
   
   echo "git:publish in $root with message $msg"
   dir:push "$root"

   git:add:all       || return
   git:commit "$msg" || return

   git pull --no-edit
   git:push:all
   dir:pop
}
