command:remove() {
   local name="$1"
   if [[ -z "$name" ]] ; then
      echo "Usage: command:remove <name> "
      return 1
   fi
   git rm ~/command/$name
   command:publish "Removed command $name"
}
