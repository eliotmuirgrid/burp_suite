command:change() {
    local comment="$*"

    if [[ -z "$comment" ]]; then
        echo "Usage: change:change <comment>"
        return 1
    fi

    git add -A || return 1
    git commit -m "$comment" >/dev/null || return 1
    git push >/dev/null || return 1

    echo "$comment"
}
